\chapter{Teorie ohledně obsahu aplikace}
\label{chap:content}

V této kapitole jsou popsány teoretické základy týkající se obsahu aplikace. Nejprve je uveden přehled různých datových struktur, které aplikace podporuje, včetně jejich vlastností, operací a požadavků na vizualizaci. Dále jsou diskutovány třídící algoritmy, které budou implementovány, a jejich význam pro pochopení základních konceptů informatiky. Nakonec je popsán přístup k vizualizaci těchto datových struktur a algoritmů, včetně použitých technik a metod pro efektivní zobrazení a interakci s uživatelem.


\section{Datové struktury}
\label{sec:data-structures}

V této sekci jsou popsány datové struktury, které jsou podstatné pro oblast informatiky. Každá datová struktura je charakterizována svými vlastnostmi, operacemi a požadavky na vizualizaci.


\subsection{Lineární datové struktury}
\label{sec:linear_data_structures}

Pro účely výuky datových struktur je rozumné začít s lineárními datovými strukturami, které jsou jednodušší na pochopení a vizualizaci. Tyto struktury zahrnují mimo jiné pole, spojové seznamy, zásobníky a fronty.

I přes to, že tyto struktury jsou relativně jednoduché a již moc dobře známé, v této práci budou teoreticky uvedeny, aby byla zajištěna úplnost a konzistence s dalšími částmi práce.


\subsubsection*{Pole a spojový seznam}

Základním stavebním kamenem lineárních datových struktur jsou pole a spojové seznamy. Pole \textit{(Array)} poskytují rychlý přístup k prvkům pomocí indexů, zatímco spojové seznamy \textit{(Linked List)} umožňují dynamickou alokaci paměti a efektivní vkládání a mazání prvků pomocí ukazatelů. \customref{Obrázek}{img:arrayLinkedList} znázorňuje rozdíl mezi těmito dvěma strukturami.~\cite{levitin}


\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/theoryContent/arrayLinkedList.pdf}
    \caption{Pole a spojový seznam s n prvky}
    \label{img:arrayLinkedList}
\end{figure}

Spojový seznam je tvořen uzly, kde každý uzel obsahuje datovou část a ukazatel na další uzel v seznamu. V případě obousměrného spojového seznamu obsahuje každý uzel také ukazatel na předchozí uzel, což umožňuje obousměrnou navigaci. List musí mít ukazatel na první uzel \textit{(head)} a často také na poslední uzel \textit{(tail)} pro efektivní přidávání prvků na konec seznamu.~\cite{cormen}

Spojový seznam i pole se využívají pro implementaci abstraktnějších seznamů, které obsahují sekvenci prvků a poskytují operace pro přidávání, odebírání a přístup k prvkům.~\cite{levitin}


\subsubsection*{Zásobník a fronta}

Speciálním případem lineárních seznamů jsou zásobníky \textit{(Stack)} a fronty \textit{(Queue)}, jejichž vizualizaci je možné vidět na \customref{Obrázku}{img:stackQueue}. Jejich hlavní charakteristikou je předem specifikovaný způsob mazání a přidávání prvků.

Zásobník funguje na principu LIFO \textit{(Last In, First Out)}, což znamená, že poslední prvek přidaný do zásobníku je prvním, který je z něj odebrán. Operace přidání prvku se běžně nazývá \textit{push} a operace odebrání prvku se nazývá \textit{pop}. V zásobníku je přístup k prvkům omezen na vrchol zásobníku, jež se nazývá \textit{top}.~\cite{cormen}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/theoryContent/stackQueue.pdf}
    \caption{Zásobník a fronta}
    \label{img:stackQueue}
\end{figure}

Naopak fronta funguje na principu FIFO \textit{(First In, First Out)}, kde první prvek přidaný do fronty je prvním, který je z ní odebrán. Operace přidání prvku se nazývá \textit{enqueue} a operace odebrání prvku se nazývá \textit{dequeue}. Fronta se implementuje pomocí ukazatele, který sleduje začátek \textit{(head)} fronty, a často bývá doplněna o ukazatel na konec \textit{(tail)} fronty, což umožňuje efektivní přidávání a odebírání prvků.~\cite{cormen}

Obě struktury mohou být efektivně implementovány pomocí spojových seznamů nebo polí, v závislosti na požadavcích na výkon a paměťovou efektivitu.~\cite{levitin}


\subsection{Nelineární (stromové) datové struktury}

Mezi nelineární datové struktury patří stromy, hierarchické struktury představující spojitý acyklický graf, kde každý prvek (uzel) může mít více potomků. Stromy obecně podporují operace \textbf{vkládání}, \textbf{mazání} a \textbf{vyhledávání} prvků, přičemž složitost těchto operací závisí na konkrétním typu stromu a jeho struktuře. Tyto struktury se často používají pro reprezentaci hierarchických dat, jako jsou souborové systémy, organizační struktury nebo různé druhy indexů v databázích. V následujících podkapitolách jsou popsány různé typy stromů, které budou implementovány v aplikaci, včetně jejich vlastností, operací a požadavků na vizualizaci.


\subsubsection*{Binární vyhledávací strom}

Binární vyhledávací stromy \textit{(Binary Search Trees, BST)} jsou speciálním typem binárních stromů, kde každý uzel splňuje následující vlastnost: klíče v levém podstromu jsou menší než klíč v uzlu, a klíče v pravém podstromu jsou větší než klíč v uzlu. Tato vlastnost umožňuje efektivní vyhledávání, vkládání a mazání prvků ve stromu. Příklad binárního vyhledávacího stromu je znázorněn na \customref{Obrázku}{img:bst}.~\cite{cormen}

\begin{figure}[H]
    \centering
    \includegraphics{figures/theoryContent/bst.pdf}
    \caption{Binární vyhledávací strom}
    \label{img:bst}
\end{figure}

Efektivita operací nad BST závisí na jeho výšce. Zde nastává problém degradace BST, kdy se strom stává nevyváženým a jeho výška se blíží počtu uzlů, což vede k lineární složitosti operací. V nejhorším případě se BST může stát podobným spojovému seznamu, což výrazně snižuje jeho výkon. Degradovaný strom je možné vidět na \customref{Obrázku}{img:bst}. I přes to, že tedy operace nad BST mají průměrnou složitost $O(\log n)$, v nejhorším případě může být složitost $O(n)$. Pro zefektivnění těchto operací se používají samovyvažující se stromy, které z binárního vyhledávacího stromu vychází.~\cite{levitin}


\subsubsection*{AVL strom}

Samovyvažující se AVL stromy jsou pojmenovány po svých tvůrcích, G.~M.~Adelson-Velsky a E.~M.~Landis, kteří je představili v roce 1962. AVL strom je binární vyhledávací strom, který udržuje vyváženost tím, že zajišťuje, aby výškový rozdíl mezi levým a pravým podstromem každého uzlu byl nejvýše 1. Tato vlastnost jde vidět na \customref{Obrázku}{img:avl}, kde má každý uzel přidánu informaci o jeho vyvážení, znázorněnou jako číslo ve žlutém kruhu, které pro tento typ stromu může nabývat pouze hodnot -1, 0 nebo 1. Vyvážení je definováno jako rozdíl mezi výškou levého a pravého podstromu. V samotné implementaci se často v každém uzlu ukládá právě výška podstromu namísto vyvážení, což umožňuje efektivní výpočet vyvážení během operací vkládání a mazání. AVL stromy zajišťují, že všechny operace mají složitost $O(\log n)$, což je důvod, proč jsou často používány v situacích, kde je důležitá rychlost přístupu k datům.~\cite{levitin,cormen}

\begin{figure}[H]
    \centering
    \includegraphics{figures/theoryContent/avl.pdf}
    \caption{AVL strom}
    \label{img:avl}
\end{figure}

Vyvážení AVL stromů se udržuje pomocí rotací, které jsou prováděny po každé operaci vkládání nebo mazání, pokud dojde k narušení vyváženosti. Rotace představuje lokální transformaci podstromu právě toho vrcholu, kde je podmínka vyváženosti narušena (kde nabyla hodnoty -2 nebo 2). Existují čtyři typy rotací: jednoduchá pravá rotace, jednoduchá levá rotace, dvojitá levá-pravá a dvojitá pravá-levá rotace, přičemž levé a pravé varianty jsou zrcadlové obrazy.~\cite{levitin}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/theoryContent/avl_rotations.pdf}
    \caption{Pravá (R) a dvojitá pravá-levá (RL) rotace AVL stromu}
    \label{img:avl_rotations}
\end{figure}

Názornou ukázku pravých variant rotací (R a RL) je možné vidět na \customref{Obrázku}{img:avl_rotations}. Během rotace se mění struktura podstromu, což umožňuje obnovit vyváženost stromu a zajistit, že všechny operace zůstanou v logaritmické složitosti. Například \textbf{pravá rotace} se provádí, když levý podstrom vrcholu je příliš vysoký, a zahrnuje přesunutí levého potomka vrcholu nahoru a samotného vrcholu dolů do pravého podstromu. Akce přesunutí potomka nahoru je znázorněna oranžovou šipkou nad daným vrcholem. \textbf{Dvojitá pravá-levá rotace} se provádí, když pravý podstrom vrcholu je příliš vysoký a zároveň má levý podstrom, který je vyšší než jeho pravý podstrom. Tato rotace zahrnuje nejprve pravou rotaci na pravém potomku vrcholu a poté levou rotaci na vrcholu samotném.~\cite{levitin}

Tyto rotace sice zvyšují složitost implementace AVL stromů, ale zajišťují, že všechny operace nad stromem mají složitost $\Theta(\log n)$. Kvůli počtu rotací, které mohou být nutné po každé operaci vkládání nebo mazání, sice zabránily AVL stromům stát se standardem pro implementaci asociativních polí (\todo{lepší překlad}), ale jejich myšlenka a principy jsou základem pro další samovyvažující se stromy, které z nich vycházejí.~\cite{levitin}


\subsubsection*{Červeno-černý strom}

Dalším typem samovyvažujícího se stromu je červeno-černý strom, pojmenován podle barvy, kterou jsou označeny jeho uzly. Tento strom udržuje vyváženost pomocí pravidel týkajících se barev uzlů, což zajišťuje, že žádná cesta od kořene k listu není více než dvakrát delší než jakákoli jiná cesta. Červeno-černé stromy jsou často používány v implementacích asociativních polí, jako jsou například mapy a množiny, protože poskytují dobrý kompromis mezi složitostí implementace a výkonem. Příklad červeno-černého stromu je znázorněn na \customref{Obrázku}{img:rb}.~\cite{cormen}

\begin{figure}[h]
    \centering
    \includegraphics{figures/theoryContent/rb.pdf}
    \caption{Červeno-černý strom}
    \label{img:rb}
\end{figure}

Červeno-černé stromy jsou binární vyhledávací stromy, které musí splňovat následující pravidla, aby byla zajištěna jejich vyváženost:
\begin{enumerate}
    \item Každý uzel je buď červený, nebo černý.
    \item Kořen stromu je vždy černý.
    \item Všichni nezaplnění potomci listů jsou černí.
    \item Pokud je uzel červený, oba jeho potomci jsou černí.
    \item Každá cesta od daného uzlu k jeho potomkům obsahuje stejný počet černých uzlů.
\end{enumerate}

Dodržování těchto pravidel zajišťuje, že strom zůstává vyvážený, což umožňuje efektivní operace vkládání, mazání a vyhledávání s logaritmickou složitostí $\Theta(\log n)$. Implementace červeno-černých stromů zahrnuje rotace a změny barev uzlů, které jsou prováděny po každé operaci vkládání nebo mazání, aby se udržela vyváženost stromu. Rotace jsou podobné těm, které se používají v AVL stromech, ale pravidla pro změnu barev uzlů jsou specifická pro červeno-černé stromy a zajišťují, že strom zůstává vyvážený i v případě, že dojde k narušení pravidel během operací.~\cite{cormen}

Jako příklad operace červeno-černého stromu bude popsána implementace \textbf{vložení prvku}. Při vkládání nového prvku do červeno-černého stromu se nový uzel nejprve vloží jako červený, aby se minimalizovalo narušení vyváženosti stromu. Poté se provádí kontrola a případné opravy, které zahrnují rotace a změny barev uzlů, aby se zajistilo dodržení pravidel červeno-černého stromu. Pro ilustraci této operace je použit podstrom, kde jsou zobrazeny uzly důležité pro opravu stromu. Jedná se o čtyři uzly: nově přidaný uzel (\textbf{X}), jeho rodič (\textbf{P}), strýc (\textbf{U}) a prarodič (\textbf{G}). Konkrétní typ opravy se provádí v závislosti na barvě strýce (U) a pozici nově přidaného uzlu (X).~\cite{geeksRB}

\begin{figure}[h]
    \centering
    \includegraphics{figures/theoryContent/rb_case1.pdf}
    \caption{Oprava červeno-černého stromu po vložení prvku, případ 1}
    \label{img:rb_insertion_case1}
\end{figure}

První případ nastává, když \textbf{strýc ($U$) je červený}, jak je vidět na \customref{Obrázku}{img:rb_insertion_case1}. V tomto případě se provádí změna barev: $P$ a $U$ se zbarví na černou, zatímco $G$ se zbarví na červenou. Poté se pokračuje v kontrole a případných opravách prarodiče $G$, tato zpětná kontrola je znázorněna tlustým okrajem kolem daného uzlu. Rekurzivně se tedy strom projde až ke kořenu, aby se zajistilo, že všechna pravidla červeno-černého stromu jsou dodržena. Tento proces může vést k dalším změnám barev a rotacím, pokud dojde k narušení pravidel během opravy.~\cite{geeksRB}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/theoryContent/rb_case2.pdf}
    \caption{Oprava červeno-černého stromu po vložení prvku, případ 2}
    \label{img:rb_insertion_case2}
\end{figure}

Druhým případem je situace, kdy \textbf{strýc (U) je černý}, jak je znázorněno na \customref{Obrázku}{img:rb_insertion_case2}. V tomto případě se provádí nejen změna barev, ale i rotace. Dále se situace větví podle pozice nově přidaného uzlu (X) a jeho rodiče (P). Pro zjednodušení jsou uvedeny pouze dvě varianty, kdy je $P$ levým potomkem $G$. Pokud je nový uzel \textbf{levým potomkem} (případ L), provede se pravá rotace na $G$ a $P$ se tedy stává novým kořenem podstromu, přičemž během rotace si zmíněné vrcholy vymění barvy. Podobně jako u diagramu AVL stromu, akce přesunutí potomka směrem nahoru je na diagramu znázorněna oranžovou šipkou nad daným vrcholem. Pokud je nový uzel \textbf{pravým potomkem} (případ R), provede se nejprve levá rotace na $P$, čímž se $X$ stává levým potomkem $G$, a poté pravá rotace na $G$, přičemž během druhé rotace si $X$ a $G$ vymění barvy. Po těchto úpravách již není potřeba rekurzivní kontrola, protože všechny pravidla červeno-černého stromu jsou dodržena a strom zůstává vyvážený.~\cite{geeksRB}

Odstraňování prvku z červeno-černého stromu je složitější operace, která zahrnuje více případů a může vyžadovat několik rotací a změn barev, aby se udržela vyváženost stromu. Nicméně principy jsou podobné jako při vkládání, kdy se nejprve odstraní uzel a poté se provádí opravy, pokud dojde k narušení pravidel červeno-černého stromu.~\cite{cormen}


\subsubsection*{B-stromy}

Posledním typem datové struktury z oblasti stromů, který bude implementován v aplikaci, jsou B-stromy. B-stromy, které představili R. Bayer a E. McGreight, jsou samovyvažující se stromy navržené pro efektivní ukládání a vyhledávání dat na sekundárních úložištích, jako jsou pevné disky. V praxi je možné tyto stromy vidět primárně jako implementaci databázových indexů. B-stromy umožňují ukládání více klíčů v jednom uzlu a mají proměnný počet potomků, což zajišťuje, že strom zůstává vyvážený i při velkém množství dat.

Příklad B-stromu je znázorněn na \customref{Obrázku}{img:b_tree}, kde jde vidět struktura stromu, dost odlišná od předchozích uvedených struktur. Každý uzel může obsahovat více klíčů a mít více potomků, přičemž všechny listy jsou na stejné úrovni, což zajišťuje vyváženost stromu. B-stromy jsou navrženy tak, aby minimalizovaly počet diskových operací potřebných pro vyhledávání, vkládání a mazání dat, což je důvod, proč jsou široce používány v databázových systémech a souborových systémech.~\cite{levitin}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/theoryContent/btree.pdf}
    \caption{B-strom řádu 3}
    \label{img:b_tree}
\end{figure}

B-stromy jsou definovány parametrem $m$, který se nazývá jeho řádem a může nabývat hodnoty od 2 do nekonečna. Je nutno zmínit, že v jiné literatuře se namísto řádu používá minimální stupeň $t$, který je roven $\lceil m/2 \rceil$ a udává minimální počet potomků, které musí mít každý uzel kromě kořene a listů. Dále však v tomto textu bude použit pouze řád $m$.~\cite{levitin, cormen}

Každý uzel v B-stromu může obsahovat nejvýše $m-1$ klíčů a mít nejvýše $m$ potomků. Uzel, který není listem, musí mít alespoň $\lceil m/2 \rceil$ potomků, což zajišťuje, že strom zůstává vyvážený. Vztah mezi klíči a potomky v B-stromu je takový, že klíče v uzlu rozdělují rozsah hodnot, které mohou být uloženy v jeho potomcích, při operacích tedy slouží k navigaci stromem. Listy B-stromu jsou všechny na stejné úrovni, což znamená, že všechny cesty od kořene k listům mají stejnou délku. Operace vkládání a mazání v B-stromech zahrnují rozdělení a sloučení uzlů, aby se udržela struktura stromu a jeho vyváženost.~\cite{levitin}

Strom řádu $m$ tedy musí splňovat následující vlastnosti:
\begin{enumerate}
    \item Kořen stromu je buď list, nebo má $2$ až $m$ potomků.
    \item Každý uzel kromě kořene a listů musí mít mezi $\lceil m/2 \rceil$ a $m$ potomků.
    \item Strom je perfektně vyvážený, což znamená, že všechny listy jsou na stejné úrovni.
\end{enumerate}

\textbf{Vyhledávání} v B-stromu začíná u kořene a porovnává hledanou hodnotu s klíči v uzlu. Pokud se hodnota shoduje s některým z klíčů, operace je úspěšná a hledání končí. Pokud se hodnota neshoduje, klíče se použijí jako hraniční body určující, do kterého potomka se má hledání přesunout. Tento proces se opakuje, dokud se nenajde hledaná hodnota nebo nedojde k listu, což znamená, že hledaná hodnota není ve stromu přítomna.\cite{levitin}

Operace \textbf{vložení} listu do B-stromu je stále přímočará, vyžaduje však případné rozdělení uzlů, pokud se počet klíčů v uzlu překročí. Pokud je uzel plný (obsahuje $m-1$ klíčů) a je třeba do něj vložit nový klíč, uzel se rozdělí na dva a prostřední klíč se přesune do nadřazeného uzlu. Tento proces může rekurzivně pokračovat až k kořeni, což může vést k vytvoření nového kořene, pokud i ten bude plný. Na \customref{Obrázku}{img:btree_insert} jsou znázorněny oba případy, prvně při vložení klíče $2$ do listu, který není plný, a následně při vložení klíče $3$, kdy již dochází k rozdělení listu a přesunu klíče $3$ do nadřazeného uzlu, který se stává novým kořenem. Nově přidané klíče jsou na obrázku zvýrazněny oranžovou barvou.~\cite{levitin}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\textwidth]{figures/theoryContent/btree_insert.pdf}
    \caption{Vložení klíče do B-stromu řádu 3}
    \label{img:btree_insert}
\end{figure}

Operace \textbf{mazání} klíče z B-stromu je složitější, protože může vést k narušení struktury stromu a jeho vyváženosti. Pokud je klíč, který má být odstraněn, v listu, jednoduše se odstraní. Pokud je klíč v uzlu, který není listem, musí být nahrazen buď největším klíčem z levého podstromu (predecessor), nebo nejmenším klíčem z pravého podstromu (successor). Po odstranění klíče může dojít k situaci, kdy uzel obsahuje méně než $\lceil m/2 \rceil - 1$ klíčů, což znamená, že je potřeba provést opravu stromu. Oprava zahrnuje buď přesun klíče z přilehlého sourozeneckého uzlu (pokud má dostatek klíčů), nebo sloučení s přilehlým sourozeneckým uzlem, což může rekurzivně pokračovat až k kořeni.~\cite{levitin}

B-stromy jsou navrženy tak, aby minimalizovaly počet diskových operací potřebných pro vyhledávání, vkládání a mazání dat, což je důvod, proč jsou široce používány v databázových systémech a souborových systémech. Jejich schopnost udržovat vyváženost a efektivně spravovat velké množství dat na sekundárních úložištích je klíčovým faktorem pro jejich popularitu v těchto oblastech a proto je důležité, aby byly zahrnuty do výukové aplikace.


\subsection{Prioritní fronty a haldy}

\subsubsection*{Binární halda (Heap)}
% Specifické vlastnosti haldy, vztah mezi stromovou strukturou a jejím uložením v poli.


\section{Třídící algoritmy}
\label{sec:algorithms}

V této sekci jsou popsány třídící algoritmy, které budou implementovány v aplikaci. Každý algoritmus je charakterizován svým principem fungování, složitostí a významem pro pochopení základních konceptů informatiky.
